#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
LOG_DIR="logs/$(date -u +%Y-%m-%d)"
mkdir -p "$LOG_DIR"
exec bundle exec ruby lib/routines/weekly_digest.rb >> "$LOG_DIR/weekly-digest.log" 2>&1
